<div class="container spacing-top-1">
  <h2 class="about-header">Edit Category</h2>
  <%= simple_form_for(@category) do |f| %>
    <%= f.input :name %>
    <%= f.input :image_url %>
    <div class="d-flex justify-content-center mb-5">
      <%= f.button :submit, class: "button-blue" %>
    </div>
  <% end %>
</div>
